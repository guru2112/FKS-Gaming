# This file is only used during `npm run dev` — does NOT affect production
# Do not commit machine-local environment values in this tracked file.
# Define NEXT_PUBLIC_API_URL in an untracked local env file instead.
